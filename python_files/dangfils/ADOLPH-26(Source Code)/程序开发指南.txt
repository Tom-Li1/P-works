#安装后由一个exe检测日期，每次开机启动运行于后台，时机到位负责调用其他模块
#更换壁纸 删除桌面所有快捷方式 不是快捷方式的移动到其他位置 留下负责显示信息窗口的exe（关了再自动回弹打开）
#信息显示窗口负责计时五分钟，期间不断弹窗（各类报错弹窗）以及刷新（桌面图标闪烁），向磁盘随意位置写入无意义bat占空间
#五分钟过后，弹出‘最终の警告’后执行五个%0|%0.bat卡死。关机重启后还是如此 第二天停止发作并自我删除

初始化模块exe(检测是否有被调用历史以及日期 打开其他模块 不断向磁盘的txt中写入'槑')
更换壁纸bat(更换壁纸 被调用五次 间歇1秒 为确保winXP以上系统壁纸成功更换) done
修改桌面模块bat(清空桌面 被初始化模块创建 修改 调用 move A_path B_path) done
显示说明模块.exe(记时 被修改模块放置于桌面 关闭后初始化模块会再次打开)
弹窗骚扰模块exe(随机时间间歇 不断弹出对话框 被说明模块调用) done
卡死模块bat(五个%0|%的批处理文件 被初始化模块调用 向指定目录中写入txt标志已经被调用)
自我删除模块(删除所有相关文件 恢复桌面) done

传播模块暂不考虑
Finished on 2020 Feb 3nd